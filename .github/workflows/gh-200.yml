name: CoHack Journey

on: push

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello from GH-200 Sesion! Lets create some great Workflow files"

      - name: Checkout repo
        uses: actions/checkout@v6

      - name: List files
        run: ls -la
